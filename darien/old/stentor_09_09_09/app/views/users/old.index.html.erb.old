<% title "List Users" %>


<% for user in @users %>

  <p><%=h user.username %> <%= user.user ? "User" : "Not a User" %> <%= user.admin ? "Admin" : "Not an Admin" %> <%= user.super_admin ? "Super Admin" : "Not a Super Admin" %> </p>
  
 <%#= render :partial => 'user_list' %>
<% end %>