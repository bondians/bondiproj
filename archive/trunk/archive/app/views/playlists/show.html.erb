<p>
  <h1><%=h @playlist.name %></h1>
</p>

<p>
  <b>Tracks:</b>
<%=
  Goldberg.user.playlists.include?(@playlist) ? draw_songs(@playlist.songs, :track, :playlist_add, :playlist_del) : draw_songs(@playlist.songs, :track)
%>
</p>


<%= link_to 'Edit', edit_playlist_path(@playlist) %> |
<%= link_to 'Back', playlists_path %>
